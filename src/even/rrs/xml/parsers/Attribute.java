package even.rrs.xml.parsers;

/**
 *
 * @author even
 */
public enum Attribute
{
    attachedto,
    beam,
    camber, chord, cx, cy, cx1, cy1, cx2, cy2,
    diameter,
    id,
    length,
    name,
    point, pos,
    speed, spline2, spline3,
    twa,
    x, y

}
